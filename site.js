const toggle=document.querySelector('.mobile-toggle');const nav=document.querySelector('.nav');if(toggle&&nav){toggle.addEventListener('click',()=>{nav.classList.toggle('open');toggle.setAttribute('aria-expanded',nav.classList.contains('open'));});}document.querySelectorAll('[data-year]').forEach(el=>el.textContent=new Date().getFullYear());const form=document.querySelector('#consultation-form');if(form){form.addEventListener('submit',e=>{if(!form.action||form.action.includes('REPLACE')){e.preventDefault();document.querySelector('#form-status').textContent='Form delivery must be connected before launch. Please call 702-800-9999.';}})}
